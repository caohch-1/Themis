You are given an execution path that starts from public APIs {public_api_names} and eventually reaches target buggy statements {target_statements}. The call chain is {call_chain}. The complete source code of every function along this path is {source_codes}. Related test suite artifacts are {test_suite_snippets}.
Generate a Java integration test case that drives execution from public APIs {public_api_names} through the call chain to target statements {target_statements}. The test must initialize RPC servers with {rpc_server_code}, construct required setup and call sequences inspired by provided test suites, and invoke public APIs concurrently in separate threads. The test executes on a live system and must not contain mocking or artificial assertions.
The generated class must import io.themis.fuzz.runtime.InterleavingRuntimeHooks and io.themis.fuzz.runtime.ThemisRuntimeContext, call InterleavingRuntimeHooks.configureFromSystemProperties(), read runtime seed/selectfuzz/target-probe context through ThemisRuntimeContext, and execute probe calls on the target path using InterleavingRuntimeHooks.probe(...) or InterleavingRuntimeHooks.probeId(...).
Produce a complete Java test class named {test_name} with a main method that can be compiled and executed directly.
