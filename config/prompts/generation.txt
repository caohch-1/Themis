You are given an execution path that starts from the public APIs {public_api_names} and eventually reaches target buggy statements {target_statements}. The call chain is {call_chain}. The complete source code of every function along this path is {source_codes}.
Your task is to generate a Java integration test case that drives execution from public APIs {public_api_names} through the given call chain to target statements {target_statements}. The test must initialize RPC servers with {rpc_server_code}, construct correct call sequences for triggering this path, and invoke the public APIs concurrently in separate threads. The test executes on a live system and must not include mocking or artificial assertions.
The generated class must import io.themis.fuzz.runtime.InterleavingRuntimeHooks and io.themis.fuzz.runtime.ThemisRuntimeContext, call InterleavingRuntimeHooks.configureFromSystemProperties(), read runtime seed/selectfuzz/target-probe context through ThemisRuntimeContext, and execute probe calls on the target path using InterleavingRuntimeHooks.probe(...) or InterleavingRuntimeHooks.probeId(...).
Produce a complete Java test class named {test_name} with a main method that can be compiled and executed directly.
